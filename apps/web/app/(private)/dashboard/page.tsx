export default function DashboardPage() {
  return (
    <main style={{ maxWidth: 640, margin: "4rem auto", fontFamily: "sans-serif" }}>
      <h1>Dashboard placeholder — Phase 3</h1>
      <p>
        Reaching this page means the auth gate in <code>middleware.ts</code> let you through.
        Training program, kcal tracker, measurements, charts, and the photo gallery all land in
        Phase 3.
      </p>
    </main>
  );
}
