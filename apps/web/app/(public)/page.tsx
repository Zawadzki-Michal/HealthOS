export default function PublicHomePage() {
  return (
    <main style={{ maxWidth: 640, margin: "4rem auto", fontFamily: "sans-serif" }}>
      <h1>HealthOS</h1>
      <p>
        Public case-study page placeholder — the real aggregate trends land in Phase 4, reading
        only from <code>public_insights</code>.
      </p>
    </main>
  );
}
