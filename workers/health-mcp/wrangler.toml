name = "health-mcp"
main = "src/index.ts"
compatibility_date = "2026-08-08"

# Secrets (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, WEBHOOK_SECRET) are set via
# `wrangler secret put <NAME>` -- never listed here, never committed.
[vars]
